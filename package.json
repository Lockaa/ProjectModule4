{
  "name": "module4",
  "version": "1.0.0",
  "description": "Implement a basic user authentication system using JWT in a Node.js application. \r # Assignment:\r     1. Set up a simple Express server with routes to register and login users.\r     2. Use JWT (JSON Web Tokens) to authenticate users.\r     3. Implement registration and login endpoints.\r     4. Ensure users can only access protected routes if they are logged in (JWT required). \r # Deliverables:\r     ● A Node.js server with JWT authentication.\r     ● Registration and login routes.\r     ● A protected route that requires authentication.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2"
  }
}
